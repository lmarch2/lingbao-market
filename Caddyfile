{$DOMAIN} {
    # Backend API - proxy /api/* to backend without stripping prefix
    handle /api/* {
        reverse_proxy backend:8080
    }

    # Frontend - everything else
    handle {
        reverse_proxy frontend:3000
    }
}
